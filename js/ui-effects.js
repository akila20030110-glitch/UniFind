document.addEventListener('DOMContentLoaded',()=>{const targets=document.querySelectorAll('.action-card,.stat-card,.item-card,.mini-card,.recent-row,.claim-row,.notification-card,.admin-claim-card');const tilt=document.querySelectorAll('.action-card,.stat-card,.item-card,.mini-card');if(!window.matchMedia('(hover: hover) and (pointer: fine)').matches||window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;targets.forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top;card.style.setProperty('--mx',`${x/r.width*100}%`);card.style.setProperty('--my',`${y/r.height*100}%`)});card.addEventListener('mouseleave',()=>{card.style.setProperty('--mx','50%');card.style.setProperty('--my','50%')})});tilt.forEach(card=>{card.addEventListener('mouseenter',()=>card.classList.add('mouse-active'));card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width,y=(e.clientY-r.top)/r.height;card.style.setProperty('--rx',`${((.5-y)*7).toFixed(2)}deg`);card.style.setProperty('--ry',`${((x-.5)*7).toFixed(2)}deg`)});card.addEventListener('mouseleave',()=>{card.classList.remove('mouse-active');card.style.setProperty('--rx','0deg');card.style.setProperty('--ry','0deg')})})});